spring:
  datasource:
    url: jdbc:tc:postgresql:15:///aegisnotify
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

eureka:
  client:
    enabled: false
