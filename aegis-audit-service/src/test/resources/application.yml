spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/audit-test
  kafka:
    bootstrap-servers: localhost:9092
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8088/realms/aegis/protocol/openid-connect/certs

eureka:
  client:
    enabled: false

audit:
  encryption:
    # Test-only key — NOT for production use
    key: b44csZrjEgmHvxR/WuBXoIlOxRv63gwzWxwkiiQMPFE=
  topic: notification-audit-events
  consumer:
    group-id: audit-service-test
